import { environment } from 'src/environments/environment';

export const DEFAULT_LIMIT: number = 15;
export const URL_PROFILE_IMAGE: string = environment.api + '/files/profile-image/';
export const URL_ATTACHMENTS: string = environment.api + '/files/attachment/';